### Tuân thủ:

- docs\rule.md
- docs\Instructions.md
- docs\error-skill-devtasktracker.md (không để lỗi xảy ra)

### Thực hiện tiếp tục:

- [x]: Kiểm tra lại danh sách `.sln` và `.csproj` có lưu file để làm cache (cập nhật mới khi last edit quá 10, 20 phút chưa)

**Xác nhận:** Cache hoạt động đúng — lưu vào `solution-file-cache.json` atomic, TTL theo `SolutionFileCacheMinutes` (default 20). Bổ sung `GetCacheAgeDisplay()` để status bar hiển thị tuổi cache ("5m 20s trước").

- [x]: Cải thiện chức năng này, load rất chậm và filter cũng chậm => xử lý tối ưu.

**Triển khai:**
- `SolutionFileService.cs`: đổi từ `Directory.GetFiles` (eager) sang `Directory.EnumerateFiles` (lazy) + `Parallel.ForEach` với `ConcurrentBag<SolutionFileEntry>` — tận dụng đa nhân CPU.

- [x]: Thêm tìm kiếm nội dung bằng ripgrep.

**Triển khai:**
- `Core/Models/AppSettings.cs`: thêm field `RipgrepPath`
- `Core/Models/RipgrepSearchResult.cs`: model mới
- `ViewModels/ProjectHelperViewModel.cs`: ripgrep search với CancellationToken
- `ToolWindows/ProjectHelperControl.xaml`: Expander tìm kiếm nội dung
- `DevTaskTrackerPackage.cs`: version 3.5
- `DhCodetaskExtension.csproj`: thêm RipgrepSearchResult.cs
