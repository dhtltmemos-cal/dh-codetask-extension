### Tuân thủ:

- docs\rule.md
- docs\Instructions.md
- docs\error-skill-devtasktracker.md (không để lỗi xảy ra)

### Thực hiện tiếp tục:

- [ ]: Bổ sung thêm chức năng trong panel Tracking, các thông tin này sẽ lưu json của task
  - Khi thực hiện `Fetch` url xong, lấy các thông tin trong đó, để thể hiện một số thông tin như `username create`, `date create`
  - Lấy body của issue để trích xuất các thông tin như `ip server`, `ten database`, `port kết nối`, các thông tin này do người dùng tự gõ nên có thể không đúng theo cấu trúc nhất định, phải dùng regex, tìm chuỗi... để tìm được các thông tin này. Dưới đây là các ví dụ của raw text này. Phải dùng nhiều cách để trích xuất các thông tin này, nếu hông có thì tìm cách khác, dùng các tên để mô tả như csdl, tên database, db.... kết hợp với regex tên database phải thế nào (postgres) như không ký tự đặc biệt, chuỗi không dấu.... port thì toàn số,.... (thông tin ip, tên máy chủ có cấu hình trong settings, vì các task trong nhóm chủ yếu là 192.168.50.79, nên trong raw text có thể hông có cái thông tin này)

  ```
    ten_csdl tra_minhquang_full_13032026
    IP Server 192.168.50.79
    port_csdl 5437
  ```

  ```
    🗄️ Thông tin dữ liệu
    🧩 DB: emr_omon
    🔌 Port: 5433
  ```

  - Thêm control để thể hiện các thông tin này, có thể copy khi cần.
